function encodeAndDecodeMessages() {
  let buttons = document.querySelectorAll("button");
  let encodeButton = buttons[0];
  let decodeButton = buttons[1];


//   console.log(encodeButton);
//   console.log(decodeButton);
//   debugger;   
  let mainContainer = document.getElementById("main").children;

  let encodeMessageTextArea = mainContainer[0].children[1];
  


  function encode() {}

  function encode() {}
}
